# Browser-safe Supabase configuration.
# Copy these names into a local ignored environment file when the remote
# Development project is available.

VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Never expose privileged Supabase secret keys through VITE_* variables.
